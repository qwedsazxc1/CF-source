#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
using ll = long long;
using ld = long double;
int main(){
    int n;
    scanf("%d", &n);
    while(n--){
        int t, len;
        scanf("%d%d", &t, &len);
        char ph[t][len + 5];
        for (int i = 0; i < t;i++){
            scanf("%s", ph[i]);
        }
        char q[len + 5];
        scanf("%s", q);
        
    }
    return 0;
}